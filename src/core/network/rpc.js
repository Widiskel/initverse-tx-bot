import { Config } from "../../../config/config.js";

export class RPC {
  static CHAINID = Config.RPC.CHAINID;
  static RPCURL = Config.RPC.RPCURL;
  static EXPLORER = Config.RPC.EXPLORER;
  static SYMBOL = Config.RPC.SYMBOL;
}
